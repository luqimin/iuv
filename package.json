{
  "name": "root",
  "scripts": {
    "start": "lerna run start --stream --parallel --no-bail",
    "lint": "lerna run lint",
    "clean": "lerna run clean",
    "prebuild": "npm run lint && npm run clean",
    "build": "lerna run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/luqimin/iuv.git"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
